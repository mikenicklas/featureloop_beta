.row
  .col-md-4.col-md-offset-4.card--white
    h2.text-center.u-mb--2 Sign up
    = bootstrap_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = devise_error_messages!
      = f.email_field :email, autofocus: true, autocomplete: "email"
      = f.password_field :password, autocomplete: "new-password", help: "(#{@minimum_password_length} characters minimum)"
      = f.password_field :password_confirmation, autocomplete: "new-password"
      .actions.u-mt--2
        = f.submit "Sign up", class: "button--primary button--block"
    = render "devise/shared/links"
